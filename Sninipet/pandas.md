1. parquet is best file type for performance
2. max col/row

   ```python
   # Set maximum columns/rows to display as None (show all columns/rows)
   pd.set_option('display.max_columns', None)
   pd.set_option('display.max_rows', 36)
   ```
3. `<snap style="color:red"> for markdown text in color`
4.
